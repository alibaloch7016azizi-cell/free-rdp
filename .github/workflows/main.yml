name: Simple Working RDP

on:
  workflow_dispatch:

jobs:
  rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
    - name: Enable RDP
      run: |
        Write-Host "🔧 Setting up RDP..."
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        $password = ConvertTo-SecureString "Simple123!" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $password
        Write-Host "✅ RDP enabled!"

    - name: Download Tunnel
      run: |
        Write-Host "📥 Downloading tunnel..."
        Invoke-WebRequest -Uri "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe" -OutFile "tunnel.exe"
        Write-Host "✅ Downloaded!"

    - name: Start Tunnel
      run: |
        Write-Host "🚀 Starting tunnel..."
        Write-Host ""
        
        # شروع tunnel به صورت مستقیم
        & ".\tunnel.exe" tunnel --url tcp://localhost:3389

    - name: Keep Alive (This won't run but needed)
      run: |
        Write-Host "Session active..."
        Start-Sleep -Seconds 21000
